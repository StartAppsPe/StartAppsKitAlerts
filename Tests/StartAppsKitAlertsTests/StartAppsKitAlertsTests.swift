import XCTest
@testable import StartAppsKitAlerts

class StartAppsKitAlertsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        // XCTAssertEqual(StartAppsKitAlerts().text, "Hello, World!")
    }


    static var allTests : [(String, (StartAppsKitAlertsTests) -> () throws -> Void)] {
        return [
            ("testExample", testExample),
        ]
    }
}
